{
  "name": "holiday-best-fit",
  "version": "1.0.0",
  "private": true,
  "description": "AI holiday outbound travel decision assistant demo.",
  "scripts": {
    "start": "node server.cjs",
    "check": "node --check server.cjs && node --check app.js && node --check tests/smoke.cjs"
  },
  "engines": {
    "node": ">=20 <25"
  }
}
