# OTA 国际机票接入矩阵

更新日期：2026-07-19

| 平台 | 官方能力核实结果 | 实时搜索 | 官网跳转 | V2 状态 |
| --- | --- | --- | --- | --- |
| 同程旅行 / 艺龙 | 官方开放平台提供 `air.iflight.search` 国际机票搜索和 `air.iflight.verify` 验价；账号与密钥由商务发放 | 可接，需商务凭证 | 官方支持合作伙伴 H5 链接模式，具体行程深链需商务配置 | 已实现服务端适配器 |
| 携程 | 已找到携程商旅开放平台和国际机票供应商接口，未核实到匿名消费者比价接口 | 需商务确认 | 需商务提供可归因深链 | 仅展示接入状态 |
| 去哪儿 | 未核实到公开的消费者国际机票实时搜索 API | 暂不可接 | 需商务确认 | 仅展示接入状态 |
| 美团 | 开放平台采用合作申请和技术方案评审流程，未核实到公开机票搜索接口 | 需商务合作 | 需商务确认 | 仅展示接入状态 |
| 飞猪 | 官方机票 API 需要商家入驻和接口授权，公开能力以供给、政策和订单为主 | 需商家/商务授权 | 需商务确认 | 仅展示接入状态 |

## 报价选择规则

1. 只把 OTA 服务端成功返回的数据标记为实时价。
2. 优先选择抓取时间在 15 分钟内的实时报价。
3. 同等新鲜度下，优先支持跳转 OTA 官方页面核验的报价。
4. 再比较成人含税价；页面始终展示平台和抓价时间。
5. 无实时报价时回落到产品演示样例，禁用官网核验按钮。

## 同程接入配置

服务端需要 `TONGCHENG_USER`、`TONGCHENG_APP_KEY` 和 `TONGCHENG_SECRET_KEY`。如果商务同时提供行程级 H5 深链规则，再配置 `TONGCHENG_DEEP_LINK_TEMPLATE`；没有模板时仍可展示实时价格，但会明确标记“暂不可核验”。

接口密钥仅允许放在服务端环境变量中，不得写入 `app.js` 或发送到浏览器。
